server.port=${PORT:8016}
# Database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL94Dialect
spring.datasource.driver-class-name=org.postgresql.Driver
# jdbc:postgresql://ip:port/database_name
spring.datasource.url=jdbc:postgresql://${DB_HOST:127.0.0.1}:${DB_PORT:5432}/adcmodule
spring.datasource.username=postgres
spring.datasource.password=IONkinaM-2102
spring.jpa.hibernate.ddl-auto=update
# Files
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:50MB}
spring.servlet.multipart.max-request-size=${MAX_FILE_SIZE:50MB}
#MVC
spring.mvc.pathmatch.matching-strategy=ant_path_matcher